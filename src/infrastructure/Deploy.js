import { getAppContext } from "./ApplicationDbContext.js";

/* Cleanup

*/

export function DeployToSite() {
  const _context = getAppContext();

  // 1. Create our configuration lists

  // ConfigRequestOrgs
  // ConfigServiceTypes
  // ConfigPipelines
  // ConfigHolidays

  // 2. Create our entity lists

  // 3. Create our pages
}
